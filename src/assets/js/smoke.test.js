import {
    beforeEach,
    describe,
    expect,
    it,
} from 'vitest';

describe('Unit tests run?', () => {
    beforeEach(() => {
    });

    it('Ran.', () => {
        expect(true).toEqual(true);
    });
});
